name: app_638
description: 638 — Ropa Urbana

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.2

  # Navigation
  go_router: ^13.2.0

  # HTTP
  http: ^1.2.1

  # Local storage (token auth)
  shared_preferences: ^2.2.3

  # Fonts
  google_fonts: ^6.2.1

  # Images
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
